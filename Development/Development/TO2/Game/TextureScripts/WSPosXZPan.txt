UserParams XSpeed, ZSpeed, XScale, ZScale, XOffset, ZOffset;
Input  WSPos;
Output TexCoord2;
BeginScript
	# Apply the scale on the coordinates
	Mat00 = XScale;
	Mat11 = 0.0;
	Mat12 = ZScale;

	# Apply the translation
	Mat03 = XOffset + ((Time * XSpeed) % 1.0);
	Mat13 = ZOffset + ((Time * ZSpeed) % 1.0);
EndScript