# RegMgr32 — desacoplado (graphify wt1-2, no crítico para demos)
cmake_minimum_required(VERSION 3.27)
add_library(RegMgr32 INTERFACE)
message(STATUS "RegMgr32 desacoplado: Windows Registry -> std::filesystem")
